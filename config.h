#ifndef __CONFIG_H
#define __CONFIG_H

#define MAX_PROCESSES 20 // Absolute maximum processes that can be run
#define MAX_TIMEOUT 300 // Max time in seconds until timeout (5 min)
#define LOG_FILE "logfile" // File to output log messages to
#define SHM_FILE "shmlicense.shm" // File which serves as a shared memory link

#endif
